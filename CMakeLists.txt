# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camstreamerTaskLib)
ADD_LIBRARY(${CAMSTREAMER_TASKLIB_NAME} SHARED 
    ${CAMSTREAMER_TASKLIB_SOURCES})
add_dependencies(${CAMSTREAMER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${CAMSTREAMER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${CAMSTREAMER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMSTREAMER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMSTREAMER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMSTREAMER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMSTREAMER_TASKLIB_HEADERS}
    DESTINATION include/orocos/camstreamer)

